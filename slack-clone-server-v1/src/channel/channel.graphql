

type Channel{
  id: Int!
  name: String!
  isPublic: Boolean!
  messages: [Message!]!
  users: [User!]!
}

type Mutation{
  createChannel(teamId:Int!, name: String!, isPublic: Boolean=false): Boolean! 
}